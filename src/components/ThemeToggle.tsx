
// Null ThemeToggle (no-op): Disables dark mode switching, app is always light.
import * as React from "react";

export default function ThemeToggle() {
  // Does not render anything, disables all theming logic.
  return null;
}
